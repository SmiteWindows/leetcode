// https://leetcode-cn.com/problems/number-of-squareful-arrays/
pub fn num_squareful_perms(a: Vec<i32>) -> i32 {
    todo!()
}
// math backtracking graph
#[test]
#[ignore]
fn test2_996() {
    assert_eq!(num_squareful_perms(vec![1, 17, 8]), 2);
    assert_eq!(num_squareful_perms(vec![2, 2, 2]), 1);
}
