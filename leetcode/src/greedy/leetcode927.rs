// https://leetcode-cn.com/problems/three-equal-parts/
pub fn three_equal_parts(a: Vec<i32>) -> Vec<i32> {
    todo!()
}
// math binary_search greedy
#[test]
#[ignore]
fn test3_927() {
    assert_eq!(three_equal_parts(vec![1, 0, 1, 0, 1]), vec![0, 3]);
    assert_eq!(three_equal_parts(vec![1, 1, 0, 1, 1]), vec![-1, -1]);
}
