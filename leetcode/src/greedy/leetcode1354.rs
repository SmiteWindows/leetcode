// https://leetcode-cn.com/problems/construct-target-array-with-multiple-sums/
pub fn is_possible(target: Vec<i32>) -> bool {
    todo!()
}
// greedy
#[test]
#[ignore]
fn test1_1354() {
    assert_eq!(is_possible(vec![9, 3, 5]), true);
    assert_eq!(is_possible(vec![1, 1, 1, 2]), false);
    assert_eq!(is_possible(vec![8, 5]), true);
}
