// https://leetcode-cn.com/problems/cracking-the-safe/
pub fn crack_safe(n: i32, k: i32) -> String {
    todo!()
}
// math depth_first_search
#[test]
#[ignore]
fn test1_753() {
    assert_eq!(crack_safe(1, 2), "01".to_string());
    assert_eq!(crack_safe(2, 2), "00110".to_string());
}
